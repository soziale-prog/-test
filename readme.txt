## 👨‍💻 Автор CJ
## 🚀 Возможности

1. **Авторизация через VK Bridge**
   - При запуске приложение автоматически получает имя и аватар пользователя.
   - Приветствие “Привет, <имя>!” и фото профиля отображаются в шапке.

2. **Работа с базой данных Supabase**
   - События хранятся в таблице `events`.
   - Каждое событие содержит поля:
     - `vk_id` — идентификатор пользователя VK;
     - `title` — название события;
     - `event_date` — дата напоминания.
   - Добавление, редактирование и удаление происходят напрямую через Supabase API.

3. **Интерфейс**
   - Полностью адаптивный дизайн (десктоп + мобильный WebView VK).
   - Поля: “Название события”, “Дата”, кнопка “Добавить”.
   - Удобный список событий:
     - ✏️ редактирование события в строке;
     - ❌ удаление;
     - 💾 сохранение изменений.
   - На мобильных устройствах поля и кнопки увеличены для удобства касания.
   - Шапка с аватаром и именем пользователя автоматически перестраивается (рядом на ПК, над текстом — на телефоне).

## ⚙️ Техническая структура
/-test/
├── index.html # Основной HTML-файл приложения
├── style.css # Десктопный стиль
├── style-mobile.css # Мобильная адаптация
└── README.md # Описание проекта

**Основные технологии:**
- HTML + CSS + JavaScript (чистый фронтенд, без фреймворков)
- [VK Bridge](https://github.com/VKCOM/vk-bridge) — API для взаимодействия с ВКонтакте
- [Supabase](https://supabase.com) — база данных PostgreSQL + REST API
- Хостинг: [GitHub Pages](https://pages.github.com)

---

## 🧭 Логика работы

1. При загрузке страницы выполняется `loadUser()`:
   - инициализируется VK Bridge;
   - загружаются имя и фото пользователя;
   - приветствие обновляется в шапке.
2. Выполняется `loadEvents()` — загрузка событий пользователя из таблицы Supabase.
3. При нажатии на кнопку **“Добавить”**:
   - создаётся новая запись (`title`, `date`, `vk_id`);
   - обновляется список событий.
4. Редактирование и удаление событий выполняются напрямую через API Supabase.
5. Интерфейс адаптируется под ширину экрана:
   - до 700px активируется `style-mobile.css`;
   - аватар и имя центрируются, поля увеличиваются.

## 💡 Возможные улучшения

- 🔔 Подсветка событий “Сегодня” и “Завтра”
- 🌓 Автоматическая тёмная тема (через `prefers-color-scheme`)
- 📤 Экспорт событий (CSV или JSON)
- 📱 Добавление локальных уведомлений для напоминаний

---




